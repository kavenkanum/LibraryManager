@model LibraryMVC.Controllers.BorrowBookViewModel
@{
    ViewData["Title"] = "Książka wypożyczona";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Book.Name</h2>


<p>Aby wypożyczyć kolejną książkę naciśnij przycisk wróć.</p>

<a href="@Url.Action("SelectBook", "BorrowBook", new { forUserId = Model.UserId })" class="btn btn-default">Wróć</a>
<a href="@Url.Action("Index", "Home")" class="btn btn-default">Strona główna</a>

